import * as React from "react";
import { Typography } from "antd";

const { Title } = Typography;

function Header() {
  return <Title level={4}>결정하기 어려울 땐 결정맨!! 호옷?!</Title>;
}

export default Header;
